spring:
  application:
    name: cloud-nacos-config
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        prefix: cloud-nacos-config
        group: nacos-config
        file-extension: properties